/**
 * @file app.routes.ts
 * @description Defines the routes for the Angular application. This file configures the application's navigation paths
 * and connects components to specific URLs.
 */

import { Routes } from '@angular/router';

/**
 * @constant {Routes} routes
 * @description An array of route configurations for the Angular application.
 * This constant will define the application's routing paths and link them to respective components.
 * Initially, it is empty and should be populated with route objects that specify path and component mappings.
 */

export const routes: Routes = [];
